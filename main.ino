// Include Hardware API libraries
// Custom modules import - a) Auto IMU Calibration
// module has input from the sensor IMU and gives output of change or delta or maybe new constants values for the PID.
void setup() {
  // 1) Pin Definitions
  // 2) PID constants
}

void loop() {
  // 1) Sensor data interpretation
  // 2) Point velocity calculation
  // 3) Point velocity interpretation
  // 4) Opt - PID calibration using the IMU
  // 5) Then comes the calculation of steering value
  // 6) Actuation
}
